# 2026-08-18

- 为用户生成 3 个 Telegram 名片工具授权码（30 天有效期，1 台设备）：
  - TGCARD-38Z4-3YXZ-3RNE
  - TGCARD-5UZG-LSCT-OL5I
  - TGCARD-7PSI-Z8A8-WK5S
- 修复授权码激活后提示 "Signature verification failed" 的问题：
  - 根因：license_client.py 用 LICENSE_JWT_SECRET 本地验证 JWT 签名，客户端密钥与服务器不一致导致误报。
  - 修改：移除客户端 JWT 签名验证，改为仅解析 token payload 做离线过期检查 + 授权服务器在线验证；app.py 不再读取/传递 LICENSE_JWT_SECRET。
  - 重新打包 macOS 通用版：`dist/Telegram名片工具-macOS-universal.zip`（Intel + Apple Silicon）。
